rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    // usersコレクション内の、自身のuserIdと一致するドキュメントとそのサブコレクションのみアクセス許可
    match /users/{userId}/{document=**} {
      allow read, write: if request.auth != null 
                         && request.auth.uid == userId;
    }
    
  }
}